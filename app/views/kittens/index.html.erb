<h1>All Kittens</h1>
<% @kittens.each do |kitten| %>
  <ul>
    <li><%= link_to kitten.name, kitten_path(kitten) %> | <%= link_to "Delete", kitten_path(kitten), method: :delete, data: { confirm: "Are you sure?" } %></li>
  </ul>
<% end %>
<%= link_to "Create a Kitten!", new_kitten_path %>
